#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first auto variable to hold the first number
 * @b: second auto variable to hold the second number
 *
 * Return: returns the result
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
